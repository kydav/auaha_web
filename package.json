{
  "name": "auaha-web",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "gen:states": "node scripts/generate-state-pages.mjs",
    "gen:guides": "node scripts/generate-guides.mjs",
    "gen": "npm run gen:states && npm run gen:guides",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "pmtiles": "^4.2.0"
  },
  "devDependencies": {
    "pdfkit": "^0.19.1",
    "wrangler": "^4.0.0"
  }
}
